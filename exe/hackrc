#!/bin/env rc

test -r $home/lib/profile && . $home/lib/profile

linefile = `{mktemp}
oldifs = $ifs

fn prompt {
	echo -n '% '
}

fn sigint {
}

while(true){

	prompt
	hackline >[3] $linefile
	echo

	ifs = ()
	line = `{cat $linefile}
	ifs = $oldifs

	eval $"line
}

